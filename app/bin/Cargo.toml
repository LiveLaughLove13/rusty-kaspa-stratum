[package]
name = "rustbridge-bin"
version = "1.3.0"
edition = "2021"
authors = ["RustBridge Contributors"]
description = "Binary entry point for RustBridge"
license = "MIT"

[dependencies]
rustbridge-stratum-service = { path = "../../lib/stratum-service" }
tokio = { version = "1.0", features = ["full"] }
anyhow = "1.0"
